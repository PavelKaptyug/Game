#include <iostream>
#include <SDL.h>
#include <SDL_image.h>
#include "spawn.h"
void StartSpawn()
{
    while (true)
    {
        std::cout << "chek" << std::endl;
        SDL_Delay(5000); // Задержка в 5 секунд
    }
}
